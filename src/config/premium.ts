/**
 * PREMIUM FEATURE FLAG
 *
 * Set to `true` to re-enable all premium/subscription features.
 * When `false`:
 *  - All subscription checks return free tier
 *  - Payment sheets, upgrade prompts, and locked previews are hidden
 *  - Premium nudges are suppressed
 *  - The app behaves as a fully functional free app
 */
export const PREMIUM_ENABLED = false;
